#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @n: is the int to check
 * Return:  absolute value of an int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
